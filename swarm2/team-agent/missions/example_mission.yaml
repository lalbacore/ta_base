---
mission:
  id: "mission_001"
  name: "Build REST API"
  description: "Create a FastAPI REST API with authentication"
  created_at: "2025-11-22T19:00:00Z"
  created_by: "user_123"
  
  # Mission requirements - what the user wants
  requirements:
    - "Create a FastAPI application"
    - "Add JWT authentication"
    - "Include CRUD endpoints for users"
    - "Add proper error handling"
    - "Include OpenAPI documentation"
  
  # Acceptance criteria - how we know it's done
  acceptance_criteria:
    - quality_score: ">= 80"
    - compliance_score: ">= 90"
    - test_coverage: ">= 85%"
    - security_scan: "pass"
  
  # Stage-specific instructions
  stages:
    architect:
      instructions:
        - "Use clean architecture patterns"
        - "Design for scalability"
        - "Consider microservices if appropriate"
      constraints:
        - "Must use Python 3.11+"
        - "Must be containerizable"
    
    builder:
      instructions:
        - "Follow PEP 8 style guide"
        - "Add comprehensive docstrings"
        - "Include type hints"
      output_format: "python_package"
      
    critic:
      focus_areas:
        - "code_quality"
        - "security"
        - "performance"
        - "maintainability"
      strictness: "high"
    
    governance:
      policies:
        - "no_hardcoded_secrets"
        - "require_authentication"
        - "require_tests"
      auto_approve: false
  
  # Output configuration
  output:
    type: "github_pr"
    repository: "org/repo"
    branch: "feature/api-{mission_id}"
    reviewers: ["user_123", "user_456"]
  
  # Team assignment
  team:
    id: "team_backend_001"
    name: "Backend Specialists"

# Future: This YAML will be generated by a UI form builder